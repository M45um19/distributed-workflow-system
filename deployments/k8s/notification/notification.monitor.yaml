apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: notification-service-monitor
  namespace: monitoring
  labels:
    release: kube-stack 
spec:
  selector:
    matchLabels:
      app: notification-service 
  namespaceSelector:
    matchNames:
      - default 
  endpoints:
  - port: http 
    path: /api/v1/notification/metrics
    interval: 15s